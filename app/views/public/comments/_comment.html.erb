<%= div_for comment do %>
    <div class="info">
      <%= avatar_img comment.author %><%= link_to comment.author.name %>
      hace <%= time_ago_in_words comment.created_at %>:
    </div>
    <div class="body">
      <%= comment.body %>
    </div>
<% end %>