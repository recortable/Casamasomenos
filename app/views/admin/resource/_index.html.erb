<table class="common-table zebra-striped">
  <thead>
  <tr>
    <% attributes.each do |attribute| %>
        <th><%= resource_class.human_attribute_name(attribute) %></th>
    <% end %>
    <th>&nbsp;</th>
  </tr>
  </thead>
  <tbody>
  <% collection.each do |resource| %>
      <tr>
        <% attributes.each do |attribute| %>
            <% value = resource.public_send(attribute).to_s %>
            <td><%= value.truncate(20) if value %></td>
        <% end %>
        <td>
          <%= link_to 'show', [:admin, resource] %>
          <%= link_to 'edit', [:edit, :admin, resource] %>
          <%= link_to 'destroy', [:admin, resource], method: :delete, confirm: "Are you sure?" %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
